-- ============================================
-- 申請フォーム種別ごとの追加データ格納カラム
-- ============================================
-- 各フォーム種別固有のフィールド（理由、フォロワー数、イベント情報等）を
-- JSONB で柔軟に格納する。共通フィールド（email, tiktok_username等）は既存カラムを使用。
ALTER TABLE applications ADD COLUMN form_data JSONB DEFAULT '{}';
